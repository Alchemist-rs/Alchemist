sudo: required
os:
  - linux
  - osx
language: rust
cache:
- apt
- cargo
env:
  global:
  - PATH=$PATH:~/.cargo/bin
rust:
- stable
script:
- cargo install diesel_cli
- diesel setup
- diesel migration run
- cargo build
notifications:
  email: false
